import React from 'react';
import { RegisterFormContainer, MenuContainer } from '../components';

export const RegisterScreen = () => (
	<React.Fragment>
		<MenuContainer />
		<RegisterFormContainer />
	</React.Fragment>
);
